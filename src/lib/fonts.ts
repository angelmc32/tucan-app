import { Georama, Inter, MuseoModerno, Raleway } from "next/font/google";

export const globalFont = Raleway({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const georamaFont = Georama({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const interFont = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const museoModernoFont = MuseoModerno({
  subsets: ["latin"],
  variable: "--font-museoModerno",
});

export const ralewayFont = Raleway({
  subsets: ["latin"],
  variable: "--font-raleway",
});
